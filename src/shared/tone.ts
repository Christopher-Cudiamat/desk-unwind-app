/**
 * - mint: eye breaks, "on" state, progress
 * - sky: movement breaks and longer resets
 * - warm: Smart Pause "waiting" and celebrations
 */
export type Tone = 'mint' | 'sky' | 'warm'

export const tones: readonly Tone[] = ['mint', 'sky', 'warm']
